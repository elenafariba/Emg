{
    "name": "emergency",
    "forwardPorts": [3000],
    "portsAttributes": {
        "3000": {
            "label": "App",
            "onAutoForward": "openBrowser",
            "visibility": "public"
        }
    },
    "postCreateCommand": "npm install",
    "postAttachCommand": "pgrep -f 'node server.js' > /dev/null || (nohup node server.js > /tmp/server.log 2>&1 &); gh codespace ports visibility 3000:public -c $CODESPACE_NAME || true"
}
